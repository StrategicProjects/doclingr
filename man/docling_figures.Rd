% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{docling_figures}
\alias{docling_figures}
\title{Extract figures (pictures) from a converted document}
\usage{
docling_figures(x, image_dir = NULL, format = "png")
}
\arguments{
\item{x}{A \code{docling_document} from \code{\link[=docling_convert]{docling_convert()}}.}

\item{image_dir}{Optional directory to save picture images into. Created if
it does not exist. Requires \code{\link[=docling_convert]{docling_convert()}} to have been called with
\code{images = TRUE}; otherwise image data is unavailable and \code{image_path} is
\code{NA} with a warning.}

\item{format}{Image file format when saving, for example \code{"png"} (default)
or \code{"jpeg"}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per figure and columns:
\itemize{
\item \code{figure_id} — 1-based index.
\item \code{caption} — caption text (empty string if none).
\item \code{page} — page number the figure appears on (\code{NA} if unknown).
\item \code{image_path} — path to the saved image, or \code{NA} if not saved.
}
}
\description{
Return a tidy tibble of the pictures Docling detected, with their captions
and page numbers. When \code{image_dir} is supplied and the document was converted
with \code{images = TRUE}, each picture is written to disk and its path returned.
}
\examples{
\dontrun{
doc <- docling_convert("paper.pdf", images = TRUE)
figs <- docling_figures(doc, image_dir = "figures")
figs$image_path
}
}
\seealso{
\code{\link[=docling_convert]{docling_convert()}}
}
