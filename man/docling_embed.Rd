% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{docling_embed}
\alias{docling_embed}
\title{Attach embeddings to chunks}
\usage{
docling_embed(chunks, embedder, text_column = "text", batch_size = NULL)
}
\arguments{
\item{chunks}{A tibble of chunks, typically from \code{\link[=docling_chunk]{docling_chunk()}}.}

\item{embedder}{A function taking a character vector and returning either a
numeric matrix with one row per input, or a list of equal-length numeric
vectors.}

\item{text_column}{Name of the column to embed. Defaults to \code{"text"}.}

\item{batch_size}{Optional integer; if set, \code{embedder} is called on
successive batches of at most this many texts and the results concatenated.
Useful for APIs with per-request limits. \code{NULL} (default) embeds in one call.}
}
\value{
\code{chunks} with an added \code{embedding} list-column of numeric vectors,
and an \code{n_dim} integer column giving each embedding's length.
}
\description{
Embed a chunk tibble's text with a user-supplied embedding function and
return the tibble with an \code{embedding} list-column. doclingr stays
provider-agnostic: you bring the \code{embedder} (an OpenAI/Cohere/Ollama API
call, a local sentence-transformers model via reticulate, anything), and this
helper handles batching, validation and tidy assembly.
}
\examples{
\dontrun{
chunks <- docling_chunk(docling_convert("paper.pdf"), max_tokens = 512)

# Any embedder: here a toy one
embed_fn <- function(txt) matrix(stats::runif(length(txt) * 8), nrow = length(txt))
docling_embed(chunks, embed_fn)
}
}
\seealso{
\code{\link[=docling_chunk]{docling_chunk()}}
}
